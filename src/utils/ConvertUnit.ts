export const convert = {
  hectogramToKilogram(value: number): string {
    return (value / 10).toFixed(1);
  },

  decimeterToMeter(value: number): string {
    return (value / 10).toFixed(1);
  },
};
